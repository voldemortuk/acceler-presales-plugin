# Generation Learnings — Assignments

Directives for an assignment-generation agent, promoted from patterns confirmed in `assignment-reviewer`'s fix-loop history per the promotion rule in `README.md`. Not a reviewer rubric — those live in `assignment-review/SKILL.md`.

## Entries

*(none yet — populate once assignment generation is connected and has real fix-loop history to draw from)*

Entry format once populated:
```
- [date] Pattern: <what kept failing> — Directive: <what generation should do instead> — Evidence: <N occurrences, engagements/dates>
```
