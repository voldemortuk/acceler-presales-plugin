# Generation Learnings — Lesson Plans

Directives for a lesson-plan-generation agent, promoted from patterns confirmed in `lesson-plan-reviewer`'s fix-loop history per the promotion rule in `README.md`. Not a reviewer rubric — those live in `lesson-plan-review/SKILL.md`.

**One candidate seed entry, not yet promoted:** row-duration sums not matching stated day length — confirmed as a real, previously-unchecked gap across every sampled Lesson Plan (none of them verified this math). Since this is a mechanical arithmetic check, generation could compute and self-verify this at creation time rather than waiting for review to catch it.

## Entries

*(promoted entries go here once real generation fix-loop history exists)*

Entry format once populated:
```
- [date] Pattern: <what kept failing> — Directive: <what generation should do instead> — Evidence: <N occurrences, engagements/dates>
```
