# Generation Learnings — Decks

Directives for a deck-generation agent, promoted from patterns confirmed in `deck-reviewer`'s fix-loop history per the promotion rule in `README.md` (same rule fails ≥3 times across different generated decks). Not a reviewer rubric — those live in `deck-review/SKILL.md`. This is what generation should do proactively so review has less to catch.

## Entries

*(none yet — populate once deck-generation is connected and has real fix-loop history to draw from)*

Entry format once populated:
```
- [date] Pattern: <what kept failing> — Directive: <what generation should do instead> — Evidence: <N occurrences, engagements/dates>
```
