# Generation Learnings — Capstone / Multi-Milestone Projects

Directives for a project-generation agent, promoted from patterns confirmed in `project-reviewer`'s fix-loop history per the promotion rule in `README.md`. Not a reviewer rubric — those live in `project-review/SKILL.md`.

**One candidate seed entry, not yet promoted:** the compliance-vs-verified-enforcement distinction (§1.2 of the reviewer skill) is important enough that generation should probably build it in from the start rather than wait for 3 fix-loop occurrences — a project brief with planted traps but no forced-and-observed enforcement check is a structural gap, not a minor omission.

## Entries

*(promoted entries go here once real generation fix-loop history exists)*

Entry format once populated:
```
- [date] Pattern: <what kept failing> — Directive: <what generation should do instead> — Evidence: <N occurrences, engagements/dates>
```
