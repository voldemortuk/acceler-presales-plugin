# Generation Learnings — Notebooks / Code Labs / No-Code Build Guides

Directives for a code-demo-generation agent, promoted from patterns confirmed in `code-demo-reviewer`'s fix-loop history per the promotion rule in `README.md`. Not a reviewer rubric — those live in `code-demo-review/SKILL.md`.

## Entries

*(none yet — populate once code-demo generation is connected and has real fix-loop history to draw from)*

Entry format once populated:
```
- [date] Pattern: <what kept failing> — Directive: <what generation should do instead> — Evidence: <N occurrences, engagements/dates>
```
